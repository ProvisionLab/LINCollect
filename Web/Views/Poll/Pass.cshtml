@model Web.Models.ViewModels.PreviewView

@using (Html.BeginForm("Pass", "Poll", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    @Html.Partial("Poll", Model, new ViewDataDictionary(ViewData) { { "region", "Html" } })

    @section scripts {
        @Html.Partial("Poll", Model, new ViewDataDictionary(ViewData) { { "region", "Scripts" } })
    }
}